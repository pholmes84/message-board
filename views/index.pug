extends layout

block content
  div.container
    h1= title
    p Welcome to #{title}
    a(href="/new")
      button.btn= "add message"
    div.container 
      each m in messages
        div.message
          div= m.text
          div= m.user
          div= m.added
